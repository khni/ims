
enum ResourceName {
  user
  role
  item
  invoice
  customer
  supplier
  purchaseOrder
  salesOrder
  warehouse
  organization
  organizationUser
  report
}

model Resource {
  id          String       @id @default(uuid())
  name        ResourceName @unique
  description String?
  permissions Permission[]
  sidebarOption SidebarOption?

}
